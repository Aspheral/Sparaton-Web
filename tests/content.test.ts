import { describe,expect,it } from 'vitest';import { kayChess,services } from '../packages/content/src/index';describe('structured content',()=>{it('qualifies Kay strength estimates',()=>{const elo=kayChess.metrics.find(m=>m.key==='estimated-elo-auto-tune');expect(elo?.status).toBe('provisional');expect(elo?.qualifier?.toLowerCase()).toContain('not a universal rating')});it('does not invent a Kay release URL',()=>{expect(kayChess.releaseUrl).toBeUndefined()});it('keeps services inquiry-driven',()=>{expect(services.length).toBeGreaterThan(3)})});
