import { describe,expect,it } from 'vitest';import { ACTIVE_TICKET_STATUSES,normalizeEmail } from '../packages/database/src/index';describe('ticket policy helpers',()=>{it('normalizes requester email consistently',()=>expect(normalizeEmail('  Example@Sparaton.COM ')).toBe('example@sparaton.com'));it('treats waiting states as active',()=>{expect(ACTIVE_TICKET_STATUSES).toContain('awaiting_staff');expect(ACTIVE_TICKET_STATUSES).toContain('awaiting_client');expect(ACTIVE_TICKET_STATUSES).not.toContain('closed')})});
