import { describe,expect,it } from 'vitest';import { hashToken,randomToken } from '../workers/api/src/security';describe('ticket token primitives',()=>{it('creates nontrivial random tokens',()=>{const a=randomToken(),b=randomToken();expect(a.length).toBeGreaterThan(30);expect(a).not.toBe(b)});it('hashes deterministically with a pepper without returning the raw token',async()=>{const raw='secret-ticket-token',pepper='test-pepper-that-is-not-production';const a=await hashToken(raw,pepper),b=await hashToken(raw,pepper);expect(a).toBe(b);expect(a).not.toContain(raw);expect(await hashToken(raw,pepper+'2')).not.toBe(a)})});
