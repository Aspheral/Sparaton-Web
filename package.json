{
  "name": "sparaton-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "npm@10.9.2",
  "workspaces": ["apps/*", "packages/*", "workers/*"],
  "scripts": {
    "dev:studios": "npm --workspace @sparaton/studios run dev",
    "dev:aspheral": "npm --workspace @sparaton/aspheral run dev",
    "dev:ilmp": "npm --workspace @sparaton/ilmp run dev",
    "dev:admin": "npm --workspace @sparaton/admin run dev",
    "dev:api": "npm --workspace @sparaton/api run dev",
    "prebuild": "node scripts/generate-brand-assets.mjs",
    "build": "npm run build -ws --if-present",
    "check": "npm run check -ws --if-present",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "validate:staging": "node scripts/validate-deployment-config.mjs --environment staging",
    "validate:production": "node scripts/validate-deployment-config.mjs --environment production",
    "backup:staging": "node scripts/backup-d1.mjs staging",
    "backup:production": "node scripts/backup-d1.mjs production",
    "deploy:staging": "node scripts/deploy.mjs staging"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@cloudflare/workers-types": "^5.20260729.1",
    "@playwright/test": "^1.62.0",
    "typescript": "^5.9.2",
    "vitest": "^4.1.10"
  }
}
