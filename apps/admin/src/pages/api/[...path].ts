import type { APIRoute } from 'astro';
const API_ORIGIN='https://api.sparaton.com';
export const ALL:APIRoute=async({request,params})=>{if(request.headers.get('x-requested-with')!=='sparaton-admin')return new Response('Forbidden',{status:403});const assertion=request.headers.get('cf-access-jwt-assertion');if(!assertion)return new Response('Cloudflare Access authentication required',{status:401});const path=params.path??'';const source=new URL(request.url);const target=new URL(`/v1/admin/${path}`,API_ORIGIN);target.search=source.search;const headers=new Headers();headers.set('cf-access-jwt-assertion',assertion);const contentType=request.headers.get('content-type');if(contentType)headers.set('content-type',contentType);const method=request.method.toUpperCase();const body=['GET','HEAD'].includes(method)?undefined:await request.arrayBuffer();const response=await fetch(target,{method,headers,body,redirect:'manual'});const outHeaders=new Headers(response.headers);outHeaders.set('cache-control','no-store');outHeaders.delete('set-cookie');return new Response(response.body,{status:response.status,headers:outHeaders});};
